const KEY_PREFIX='solopro-smart-messages-trial-v1:';
function key(email){return `${KEY_PREFIX}${String(email||'').trim().toLowerCase()}`}
function statusFrom(startedAt){if(!startedAt)return {started:false,active:false,expired:false,daysLeft:0,startedAt:null};const timestamp=typeof startedAt==='number'?startedAt:Date.parse(startedAt);if(!Number.isFinite(timestamp))return {started:false,active:false,expired:false,daysLeft:0,startedAt:null};const elapsed=Math.max(0,Date.now()-timestamp);const daysLeft=Math.max(0,Math.ceil((7*24*60*60*1000-elapsed)/(24*60*60*1000)));const expired=elapsed>=7*24*60*60*1000;return {started:true,active:!expired,expired,daysLeft,startedAt:timestamp}}
export function getSmartTrialStatus(email,serverStartedAt=null){if(serverStartedAt)return statusFrom(serverStartedAt);if(!email)return statusFrom(null);try{return statusFrom(localStorage.getItem(key(email)))}catch{return statusFrom(null)}}
export function startSmartTrial(email,startedAt=Date.now()){const current=getSmartTrialStatus(email);if(current.started)return current;try{localStorage.setItem(key(email),String(startedAt))}catch{}return statusFrom(startedAt)}
export function smartTrialStorageKey(email){return key(email)}
