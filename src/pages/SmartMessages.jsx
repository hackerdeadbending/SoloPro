import {useEffect,useMemo,useState} from 'react';
import {useApp,currencyCodeFor} from '../context/AppState';
import Icon from '../components/Icon';
import {getSmartTrialStatus,startSmartTrial} from '../utils/smartTrial';
const empty={clientId:'',date:new Date().toISOString().slice(0,10),time:'',service:'',amount:'',note:'',meetingType:'client_place',meetingPlace:''};
const dataKey=email=>`solopro-smart-messages-v1:${String(email||'guest').trim().toLowerCase()}`;
const load=email=>{try{return JSON.parse(localStorage.getItem(dataKey(email)))||[]}catch{return[]}};
const save=(email,data)=>{try{localStorage.setItem(dataKey(email),JSON.stringify(data))}catch{}};
function dateLabel(value,locale){return value?new Intl.DateTimeFormat(locale||'en-US',{weekday:'short',day:'numeric',month:'short'}).format(new Date(`${value}T12:00:00`)):'—'}
function money(app,value){return new Intl.NumberFormat(app.country.locale,{style:'currency',currency:currencyCodeFor(app.country),maximumFractionDigits:2}).format(Number(value)||0)}
function makeMessage(app,item,client){const name=client?.name||'there';const date=dateLabel(item.date,app.country.locale);const service=item.service?.trim();const time=item.time?` at ${item.time}`:'';const amount=item.amount!==''?` The total is ${money(app,item.amount)}.`:'';const note=item.note?.trim()?` ${item.note.trim()}`:'';const place=item.meetingPlace?.trim();const location=item.meetingType==='client_place'?` I’ll come to your place${place?` at ${place}`:''}.`:item.meetingType==='my_place'?` We’ll meet at my place${place?` at ${place}`:''}.`:item.meetingType==='other_place'?` We’ll meet at ${place||'the agreed location'}.`:item.meetingType==='online'?' We’ll meet online.':'';switch(app.language){case'Italian':return`Ciao ${name}! Un piccolo promemoria: ${service?`ci vediamo per ${service}${time}${item.date?` il ${date}`:''}.`:''}${location}${amount}${note} A presto!`;case'Spanish':return`¡Hola ${name}! Un pequeño recordatorio: ${service?`nos vemos para ${service}${time}${item.date?` el ${date}`:''}.`:''}${location}${amount}${note} ¡Nos vemos pronto!`;case'French':return`Bonjour ${name} ! Petit rappel : ${service?`nous nous retrouvons pour ${service}${time}${item.date?` le ${date}`:''}.`:''}${location}${amount}${note} À bientôt !`;case'German':return`Hallo ${name}! Eine kurze Erinnerung: ${service?`wir treffen uns für ${service}${time}${item.date?` am ${date}`:''}.`:''}${location}${amount}${note} Bis bald!`;case'Portuguese':return`Olá ${name}! Um pequeno lembrete: ${service?`encontramo-nos para ${service}${time}${item.date?` em ${date}`:''}.`:''}${location}${amount}${note} Até breve!`;case'Polish':return`Cześć ${name}! Krótkie przypomnienie: ${service?`widzimy się na ${service}${time}${item.date?` — ${date}`:''}.`:''}${location}${amount}${note} Do zobaczenia!`;default:return`Hi ${name}! Just a quick reminder: ${service?`we’re meeting for ${service}${time}${item.date?` on ${date}`:''}.`:''}${location}${amount}${note} Looking forward to seeing you!`}}
export default function SmartMessages(){const app=useApp();const email=app.authSession?.user?.email||app.account?.email||app.user?.email||'';const authenticated=Boolean(email);const owner=String(email).trim().toLowerCase()==='davidnostalgic@gmail.com';const [trial,setTrial]=useState(()=>getSmartTrialStatus(email));const [items,setItems]=useState(()=>load(email));const [form,setForm]=useState(empty);const [selected,setSelected]=useState(null);const [notice,setNotice]=useState('');const [trialBusy,setTrialBusy]=useState(false);const active=Boolean(owner||app.isAdmin||app.premiumActive||trial.active);useEffect(()=>{setTrial(getSmartTrialStatus(email));setItems(load(email))},[email]);useEffect(()=>{if(authenticated)save(email,items)},[email,items,authenticated]);const ordered=useMemo(()=>[...items].sort((a,b)=>`${a.date} ${a.time}`.localeCompare(`${b.date} ${b.time}`)),[items]);const chosen=items.find(x=>x.id===selected);const client=app.clients.find(x=>x.id===chosen?.clientId);const message=chosen&&client?makeMessage(app,chosen,client):'';const update=(k,v)=>setForm(x=>({...x,[k]:v}));const add=e=>{e.preventDefault();if(!form.clientId)return;const item={...form,id:`sm-${Date.now()}`};setItems(x=>[...x,item]);setSelected(item.id);setForm({...empty,date:new Date().toISOString().slice(0,10)});setNotice('Smart message prepared from the details you provided.')};const start=async()=>{if(!app.authSession?.access_token){setNotice('Your account session needs to be refreshed before starting the trial.');return}setTrialBusy(true);try{const r=await fetch('/api/smart-trial',{method:'POST',headers:{Authorization:`Bearer ${app.authSession.access_token}`}});const p=await r.json().catch(()=>({}));if(!r.ok&&r.status!==409)throw new Error(p.error||'Unable to start the trial.');const at=p.startedAt||Date.now();setTrial(startSmartTrial(email,at));setNotice(r.status===409?'Your one-time Smart Messages trial has already been used.':'Your 7-day Smart Messages trial is active.')}catch(e){setNotice(e.message||'Unable to start the trial.')}finally{setTrialBusy(false)}};
 const openChannel=channel=>{if(!message)return;if(channel==='copy'){navigator.clipboard?.writeText(message);setNotice('Message copied.');return}if(channel==='sms'){const phone=client?.socialChannel==='sms'&&client?.socialHandle?client.socialHandle:client?.phone;if(!phone){setNotice('Add a phone number to this client first.');return}window.location.href=`sms:${String(phone).replace(/[^0-9+]/g,'')}?body=${encodeURIComponent(message)}`;return}if(channel==='whatsapp'){const phone=client?.socialChannel==='whatsapp'&&client?.socialHandle?client.socialHandle:client?.phone;if(!phone){setNotice('Add a WhatsApp number to this client first.');return}window.open(`https://wa.me/${String(phone).replace(/[^0-9]/g,'')}?text=${encodeURIComponent(message)}`,'_blank','noopener,noreferrer');return}if(channel==='email'){if(!client?.email){setNotice('Add an email address to this client first.');return}window.location.href=`mailto:${client.email}?subject=${encodeURIComponent('A quick note from SoloPro')}&body=${encodeURIComponent(message)}`;return}const handle=client?.socialChannel===channel?String(client?.socialHandle||'').trim().replace(/^@/,''):'';if(!handle){setNotice(`Add the ${channel} username to this client first.`);return}navigator.clipboard?.writeText(message);window.open(`${channel==='telegram'?'https://t.me/':'https://instagram.com/'}${encodeURIComponent(handle)}`,'_blank','noopener,noreferrer');setNotice(`Message copied — open ${channel} and paste it.`)};
 if(!active)return <div className="page"><div className="page-top"><div><div className="eyebrow">SOLOPRO PREMIUM</div><h1>Smart Messages.</h1><p className="sub">Turn the client details you provide into a clear, ready-to-send message.</p></div></div><section className="panel smart-message-lock"><div className="smart-message-icon"><Icon name="message" size={25}/></div><h2>Try Smart Messages free for 7 days</h2><p>Enter the details you already have. SoloPro uses only that information to prepare the message.</p>{!authenticated?<><strong>Create a free account to start your trial.</strong><span className="modal-sub">You can keep using the rest of SoloPro as a guest.</span></>:trial.started?<strong>Your 7-day trial has ended. Upgrade to Premium to continue.</strong>:<button className="primary" onClick={start} disabled={trialBusy}>{trialBusy?'Starting…':'Start my 7-day trial'}</button>}{notice&&<div className="notice">{notice}</div>}</section></div>;
 return <div className="page"><div className="page-top"><div><div className="eyebrow">SMART MESSAGES</div><h1>Your week, ready to send.</h1><p className="sub">Add only the details you know. SoloPro prepares the wording for you.</p></div><span className="saved-pill">{app.premiumActive||app.isAdmin||owner?'Premium active':`${trial.daysLeft} days left in trial`}</span></div><div className="smart-message-grid"><section className="panel"><div className="panel-head"><div><h2>Add a message</h2><p>Choose a client, then add the details you want to use.</p></div></div><form className="form-stack" onSubmit={add}><label>Client<select required value={form.clientId} onChange={e=>update('clientId',e.target.value)}><option value="">Choose a client</option>{app.clients.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><div className="two-col"><label>Date<input type="date" value={form.date} onChange={e=>update('date',e.target.value)}/></label><label>Time<input type="time" value={form.time} onChange={e=>update('time',e.target.value)}/></label></div><label>Where will you meet?<select value={form.meetingType} onChange={e=>update('meetingType',e.target.value)}><option value="client_place">I’ll go to the client</option><option value="my_place">Client comes to me</option><option value="other_place">We’ll meet somewhere else</option><option value="online">Online</option></select></label>{form.meetingType==='other_place'&&<label>Meeting place<input value={form.meetingPlace} onChange={e=>update('meetingPlace',e.target.value)} placeholder="Cafe, studio, address…"/></label>}{(form.meetingType==='client_place'||form.meetingType==='my_place')&&<label>Location details <span className="field-hint">optional</span><input value={form.meetingPlace} onChange={e=>update('meetingPlace',e.target.value)} placeholder="Address or useful detail…"/></label>}<label>Service or task<input value={form.service} onChange={e=>update('service',e.target.value)} placeholder="Appointment, delivery, project, follow-up…"/></label><label>Price <span className="field-hint">optional</span><input type="number" min="0" step="0.01" value={form.amount} onChange={e=>update('amount',e.target.value)} placeholder="0.00"/></label><label>Extra note <span className="field-hint">optional</span><textarea value={form.note} onChange={e=>update('note',e.target.value)} placeholder="Anything else the client should know…"/></label><button className="primary full"><Icon name="spark"/>Build smart message</button></form></section><section><div className="panel-head"><div><h2>This week</h2><p>{ordered.length} planned</p></div></div>{ordered.length===0?<div className="panel empty compact"><Icon name="clock" size={25}/><strong>Your week is clear.</strong><span>Add a client and a few details to prepare your first message.</span></div>:<div className="smart-message-list">{ordered.map(item=>{const c=app.clients.find(x=>x.id===item.clientId);return <article key={item.id} className={`smart-message-item ${selected===item.id?'selected':''}`} onClick={()=>{setSelected(item.id);setNotice('')}}><div className="smart-message-meta"><span>{dateLabel(item.date,app.country.locale)}</span><span>{item.time||'Flexible'}</span></div><div className="smart-message-client"><strong>{c?.name||'Client'}</strong><span>{item.service||item.note||'Personal note'}</span></div><button type="button" aria-label="Delete smart message" className="icon-btn smart-message-delete" onClick={e=>{e.stopPropagation();setItems(x=>x.filter(v=>v.id!==item.id));if(selected===item.id)setSelected(null)}}><Icon name="trash" size={15}/></button></article>})}</div>}</section></div>{chosen&&client&&<section className="panel" style={{marginTop:14}}><div className="panel-head"><div><div className="eyebrow">READY TO SEND</div><h2>{client.name}</h2><p>Only the details you entered are used.</p></div></div><div className="smart-preview">{message}</div><div className="smart-actions"><button className="ghost-btn" onClick={()=>openChannel('whatsapp')}><Icon name="whatsapp"/>WhatsApp</button><button className="ghost-btn" onClick={()=>openChannel('sms')}><Icon name="message"/>SMS</button><button className="ghost-btn" onClick={()=>openChannel('telegram')}><Icon name="telegram"/>Telegram</button><button className="ghost-btn" onClick={()=>openChannel('instagram')}><Icon name="message"/>Instagram</button><button className="ghost-btn" onClick={()=>openChannel('email')}><Icon name="mail"/>Email</button><button className="ghost-btn" onClick={()=>openChannel('copy')}><Icon name="copy"/>Copy</button></div>{notice&&<div className="notice" style={{marginTop:10}}>{notice}</div>}</section>}</div>;
}
