import {Link} from 'react-router-dom';
import {useApp} from '../context/AppState';
import {createTranslator} from '../i18n';
export default function Cookies(){const {language}=useApp();const t=createTranslator(language);return <div className="page legal-page"><div className="page-top"><div><div className="eyebrow">SOLOPRO</div><h1>{t('cookiesTitle')}</h1><p className="sub">{t('legalUpdated')}</p></div></div><section className="panel legal-panel">{['cookiesP1','cookiesP2','cookiesP3','cookiesP4'].map(k=><p key={k}>{t(k)}</p>)}</section><Link className="text-link" to="/">{t('backToSoloPro')}</Link></div>}